import { getElement } from '../utils.js';

const cartOverlay = getElement('.cart-overlay')
const cartOverlay = getElement('.cart-overlay')
const cartOverlay = getElement('.cart-overlay')

export const openCart = () => {};
